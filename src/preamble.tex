% Packages and formatting
\usepackage[margin=1in]{geometry}
\usepackage{parskip}
\usepackage{hyperref}
\usepackage{enumitem}

% Hyperref settings
\hypersetup{
    colorlinks=true,
    urlcolor=blue,
    linkcolor=black
}

% Set no paragraph indent
\setlength{\parindent}{0pt}

% Set list style tighter
\setlist{nosep}
